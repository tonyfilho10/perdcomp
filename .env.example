# Supabase client (browser + server). Pegue em Project Settings > API Keys.
# Todo acesso a dados é via REST/HTTPS do Supabase (não Postgres direto) — ambientes
# serverless como a Netlify costumam não conseguir alcançar a porta direta do Postgres.
NEXT_PUBLIC_SUPABASE_URL="https://<project-ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<anon/publishable key>"

# Apenas server-side. NÃO prefixar com NEXT_PUBLIC_. Usado para ignorar RLS nas
# Server Actions/API routes que leem e escrevem dados.
SUPABASE_SERVICE_ROLE_KEY="<secret key>"

# Usado para assinar o cookie de sessão (login é via RPC verificar_login, não Supabase Auth).
SESSION_SECRET="<gere com: openssl rand -base64 32>"
